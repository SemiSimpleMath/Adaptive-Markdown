# Adaptive Markdown — runtime dependencies
# Python 3.10+ required.

aiohttp>=3.8,<4
claude-agent-sdk>=0.1.81,<0.2
# Block-boundary detection for the id-stamper and the inline source editor.
# A port of the same markdown-it engine the viewer renders with, so the
# backend agrees with the frontend on where each top-level block begins.
markdown-it-py>=3,<4

# Optional: enables server-side conversion of PDF / DOCX / PPTX drops
# into adaptive-markdown. If absent, those uploads return 501; .md / .tex /
# .txt / .rst / .org imports still work.
markitdown[pdf]>=0.1,<0.2
# Optional: .xlsx drops become live data-grid figures (active sheet
# extracted to CSV server-side, rendered by Tabulator in the iframe).
# Without openpyxl, .xlsx returns 501 with this install command in the
# error. CSV drops always work — they use stdlib only.
openpyxl>=3.1,<4
