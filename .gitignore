# Local-only notes
POSITIONING.md
STRATEGY.md
TODO.md
.env

# Build artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
node_modules/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor state
.vscode/
.idea/
*.swp
*~

# Runtime / generated state
screenshots/
# .history/ contains a stable baseline.md per ship-with doc (tracked in
# git, the Reset target) plus an unbounded number of timestamped
# snap-*.md captures from local agent activity (gitignored).
.history/*
!.history/*/
.history/*/snap-*.md
*.html.bak
*.id-aliases.json
*.patches/
.am-provider

# User content — created at runtime, not part of the repo
docs/
examples/raw/

# Local scratch
scratch folder/

# Saved "keeper" doc versions — agent outputs worth holding onto outside
# of .history/'s autosave churn. Local-only by convention.
keepers/

# Local-only helpers (not part of the published artifact)
convert.py
scripts/
