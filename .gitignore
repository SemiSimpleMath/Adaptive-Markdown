# Local-only notes
POSITIONING.md
STRATEGY.md
TODO.md
.env

# Build artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
node_modules/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor state
.vscode/
.idea/
*.swp
*~

# Runtime / generated state
screenshots/
.history/
*.html.bak
*.id-aliases.json
*.patches/
.am-provider

# User content — created at runtime, not part of the repo
docs/
examples/raw/

# Local scratch
scratch folder/

# Saved "keeper" doc versions — agent outputs worth holding onto outside
# of .history/'s autosave churn. Local-only by convention.
keepers/

# Local-only helpers (not part of the published artifact)
convert.py
scripts/
