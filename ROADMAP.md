# Roadmap

What's planned, what's being worked on, what's still aspirational. Ordered roughly by expected ship order, not a commitment. Open a GitHub issue if you want to push on or contribute any of these — it helps avoid duplicate work.

## Active backlog

Things we've designed and intend to ship in the next iteration or two.

### Codex runtime parity

Codex mode landed experimental in v0.1. Bringing it to feature parity with Claude mode means closing several gaps:

- **Per-edit snapshot/patch granularity.** Today Codex snapshots once per changed file per turn; Claude does this per `Edit` tool call via SDK hooks. If/when Codex CLI exposes pre/post tool hooks, the adapter should match Claude's granularity.
- **Per-turn budget cap.** Claude caps spending at `MAX_BUDGET_USD` per turn. Codex has no equivalent — a wall-clock or token-count cap belongs in the adapter to prevent runaway turns.
- **Stable JSONL event parsing.** `codex_runtime._jsonl_to_event` heuristically maps event types and will likely break if Codex CLI changes its output schema. Either pin to a known-good CLI version or fall back gracefully on unrecognized event types.
- **Agent screenshot tool for visual iteration.** Visual edits (figures, animations, page-wide CSS, embedded UI) are currently the only thing the agent can't verify on its own — it writes code and waits for the human to say "the snake is invisible." Adding a `tools/screenshot_doc.py` the agent can invoke after a visual change, plus skill guidance on when to use it, would close the perception loop. Two phases: (1) reuse the existing monitor-capture script; agent reads via the existing image-aware `Read` tool. (2) `html2canvas` inside the iframe so the agent sees only the doc, not the whole screen — smaller context, no chat-panel feedback loop, no incidental privacy leak. Codex-side support depends on whether the model has vision in CLI exec mode and whether Codex's read tool surfaces image bytes; investigate.
- **Validator retry-loop parity (deferred — has trigger).** Claude side runs `validators.py` in `PostToolUse`, reverts the file on JS/CSS/SVG errors, and pushes the validator errors back into the SDK's `additionalContext` so the agent immediately retries with structured feedback. Codex's existing per-turn write-path validator can call the same `validators.py` and revert, but cannot feed errors *back into the agent's tool-use loop* — Codex's JSONL stream is one-way, so the agent just sees a chat-side system message after the turn ends and has to be re-prompted. Closing this gap means either (a) restarting Codex with a constructed prompt that includes the validator errors, or (b) waiting for Codex CLI to expose a structured per-tool-call feedback channel. **Trigger to revisit:** Claude-side validator has been stable for 30 days *or* a Codex-using contributor opens an issue requesting it, whichever comes first.

### Authoring + reading

- **Richer inline edit.** Today readers can click into a paragraph or heading and type to edit — plaintext-only, so the source loses any `**bold**` / `_italic_` / `[link]()` markup in the edited block (formatting edits still go through Source view). Next pass: HTML → markdown round-trip via something like turndown so bold / italic / links survive, then extend the block-type allowlist to lists, blockquotes, and tables. Each new type needs a corresponding `_find_block_span` kind in the backend so the source span is replaced precisely.
- **Component library / reusable snippets.** When the agent produces something good in one doc (a snake game, a working clock, a dark-mode toggle, a stopwatch, a specific math figure), the user can save it and reuse it later in any doc — *"save this as `snake-game`"* in chat, then *"insert the snake game"* anywhere else. Storage layer: a `components/` directory at the project root, one file per component, each file is itself a valid mini adaptive-markdown doc (frontmatter with name/intent/tags/provenance + body = HTML blocks + embedded `<script>`). Key nuance: because each saved component is plain markdown, the agent can *adapt on insertion* — *"insert the snake game but in red,"* *"insert the clock with Roman numerals"* — so the unit is a seed, not a frozen widget. Open issues to settle when building: ID uniquification on insert (two copies of `<canvas id="snake-game">` collide); self-containment metadata (a snake game is self-contained, a page-walking falling-letters effect isn't — components should declare scope); CSS class namespacing to prevent style leaks; later, a viewer-side Components panel for browse + drag-insert (chat is enough for v0). Sharing is free — a `components/` dir is just files, trivially zip-able, git-able.
- **Variant blocks for audience presets.** Mark a block as variant-bearing and the agent emits sibling `<section class="variant" data-audience="novice|expert|...">` blocks. Viewer renders a tab strip; reader picks a default audience.
- **Pending changes (review-before-accept).** Toggle a viewer mode where the agent writes proposed edits to a `docs/<slug>/pending.json` sidecar; viewer overlays accept/reject UI on each proposed change. `current.md` isn't touched until accept. The current substrate is *optimistic apply* — agent edits land in `current.md` immediately and the reader either keeps them or `/undo`s. That works fine while the only "writers" are one agent + the inline-edit handler (serialized via the `state.busy` lock). The pending-changes substrate becomes load-bearing when (a) we want a review pass on long edits before they hit the live doc, or (b) human + agent edits start happening concurrently — a written design proposal will land under "Open design questions" before any implementation starts.
- **Annotation overlay.** A non-destructive layer for reader highlights, agent-marked dependencies ("blocks that depend on assumption X"), and query results. Sidecar JSON, toggleable in the viewer.
- **Spreadsheet / data-table figures (XLSX, CSV).** Next library-rendered figure type after music. Convention: `<figure class="data"><script type="text/csv">…</script></figure>` or `<script type="application/json">` for JSON datasets; viewer renders via a lazy-loaded grid library (Tabulator, AG-Grid, or a hand-rolled `<table>` for v0). Same substrate primitives the music thread paid for: `__doc.rerender` after source edits, `__doc.getRenderer` for incremental mutation (sort/filter without rewriting source), `onBeforeNodeDiscarded` keeps the rendered grid across agent edits, auto-injected per-doc `<section class="agent-skill">` on upload so the agent knows the CSV-mutation patterns without re-deriving. XLSX upload extracts to CSV or JSON server-side; live editing in the doc; agent can pivot/reshape via string-edits or via the grid's API. The mechanism generalizes further to **Plotly charts, Mermaid diagrams, Vega visualizations** — each gets its own `<figure class="…">` shape, its own renderer, its own `getRenderer` registration, its own per-doc skill block.

### Quality-of-life

- **Doc-list watchdog.** Today the viewer's doc dropdown only refreshes when files are added via the upload endpoint. A filesystem watcher would broadcast updates when `.md` files appear or disappear externally (git pull, drag in a file manager, etc.).
- **First-run setup card.** If no API key is visible at backend start, the viewer prompts for one rather than failing on the first chat turn.
- **"Save as baseline" action.** Today, manually-restored content in `docs/<slug>/current.md` (via `git checkout docs/<slug>/baseline.md`, paste from `keepers/`, an editor edit, etc.) isn't propagated to `baseline.md`. The next Reset jumps back to the original baseline, surprising the user. A small endpoint + viewer button — *"set current state as baseline"* — would overwrite `baseline.md` with the working copy. Until shipped, the workflow is `cp docs/<slug>/current.md docs/<slug>/baseline.md` from the command line.
- **Byte-exact snapshots (`save_snapshot` line-ending fix).** `save_snapshot` currently uses `Path.write_text(...)` with default `newline=None`, which on Windows translates `\n` → `\r\n` on write. Combined with `read_text(...)` doing the inverse on the way in, the captured snapshot may not byte-match the source working copy. Doesn't affect content equality (universal-newline comparisons all match), but causes spurious size deltas and `doc_changed` flicker after Reset/Restore. Fix: switch `save_snapshot` to `write_bytes` (or `write_text(..., newline="")`) and have callers pass through `read_bytes` where appropriate so the snapshot is a byte-for-byte copy of the source.

## Planned

Designed at a sketch level, not actively being implemented.

### Computation

- **In-browser Python via Pyodide.** A reserved `<div class="computation" data-lang="python">…</div>` block whose body the viewer executes in a Pyodide worker. SymPy + matplotlib available; figures with parameters can be re-run live in the doc. Adds ~6MB to first load — lazily fetched only when a doc contains a computation block.

### Multi-document

- **Cross-doc references and dependency graph.** Anchor links resolve across documents (`[Rolle's theorem](rolle-paper#main)`); the viewer surfaces backlinks (which docs reference this theorem) and a dependency-graph view (what assumptions a result depends on, traversed through links). Useful when you're working on a doc that builds on another doc you've already authored — the system tells you what cites it.
- **Browser-only deployment.** A single-file `am.html` bundling parser + viewer + agent loop calling the model API directly from JS. Loses local filesystem access; gains "share a URL, the recipient tries it in 30 seconds." Useful as the entry-point demo.

### Import

PDF and LaTeX import shipped — see "Recently shipped" below. Open items in this category:

- **Large-doc conversion: model auto-pick + chunking + continuation.** Today a ~20K-line doc converts to roughly half on Haiku because the model's per-turn output cap (~4–8K tokens depending on tier) clips the assistant message. Sonnet's 64K ceiling absorbs most papers; Opus is more. Three layered fixes when prioritized: (1) when the input source exceeds a byte threshold, auto-switch the import path to Sonnet regardless of the chat model selector — one branch in `upload_md`; (2) for server-side Claude conversion (PDF / LaTeX paths), split the input by sections / pages / headings, convert each piece, stitch, so arbitrary-size papers work without operator intervention; (3) detect `stop_reason == "max_tokens"` and re-prompt with "continue from `<last line>`" until the model returns a normal stop. Not urgent — the workaround ("switch to Sonnet in the model dropdown before importing") is one click.
- **`.rst` / `.org` via Claude.** Currently those formats route through the agent-mediated path (drop → preview → agent reads → agent writes current.md). Cheap to add to the same server-side Claude path as `.tex` (`_LLM_CONVERT_TEXT_EXTS`) once we hear demand.
- **MusicXML → ABC down-conversion.** A 4400-line piano-vocal MusicXML doc compresses to ~200-400 lines of ABC. The reader sees a near-identical rendered score (abcjs renders both well); the agent gains *actual editability* — Sonnet can fit the whole ABC in context, Opus can refactor it, even Haiku can transpose by hand. Standard tool: `xml2abc.py` (Wim Vree, single-file Python, MIT-ish). Lossy for orchestral / dense contemporary / figured-bass; essentially lossless for melody, lead sheet, folk, lieder; mostly preserved (loses engraving polish) for multi-voice piano. Ship as either an agent-callable backend route (`POST /music/xml-to-abc?slug=…`) or a sandboxed Bash invocation the agent runs itself. Per-doc decision — keep MusicXML when you need round-trip back to printed score; convert to ABC when agent-editability matters more than publication engraving.
- **DOCX / XLSX / PPTX → Claude fallback.** Today office formats go through markitdown only — quality is roughly the markitdown ceiling. The natural escalation is "if the markitdown output looks structurally poor, hand the doc to Claude with the rendered images for a second pass." Hard part is *detecting* "poor" — a `(cid:127)`-count heuristic is brittle; a "does the first paragraph parse cleanly?" check might be enough.
- **Chat-log → adaptive markdown.** Take a Slack / ChatGPT / design-review export and produce a structured doc with the substantive points extracted and the chronological log preserved as provenance.
- **Existing-`.tex`-source-of-PDF preference.** When a `.tex` source for an imported PDF is publicly available (arxiv source tarball, etc.), preferring it over the rendered-pages path would give cleaner math + bibliography. Optional second-stage import that runs only when the reader opts in.

## Directional

Likely correct, far from ready, no committed plan.

- **Lean verification.** Formal statements live alongside informal prose (`[Lean: theorem rolle : ...]`); Lean checks them; the agent keeps the formal statement in sync as the prose evolves. Proof bodies stay as `sorry`. The framing is "AI keeps the formal statement honest," not "AI proves your theorems."
- **Hosted multi-user mode.** Real-time collab, persistent storage, version-graph hosting, audit trail. Likely a separate product on top of the same format.
- **Multi-agent / multi-writer concurrency.** Real CRDT or 3-way merge for simultaneous editors. Today's optimistic-apply substrate handles solo authoring with a single agent; this is the future when a doc has multiple human + agent contributors.

## Open design questions

Decisions to make when the relevant feature lands. Captured here so contributors know they're not settled.

- **Pending-changes substrate.** *(Design pass. Implementation deferred until the design forks below are settled.)* The "Pending changes (review-before-accept)" item in Active backlog is the user-facing feature; this is where its open design decisions live.

  **What pending state actually is.** Three candidate shapes were considered: (a) a sidecar `docs/<slug>/pending.json` listing proposed edits as structured objects; (b) a parallel `docs/<slug>/pending.md` snapshot showing the doc "as if all proposed edits were applied"; (c) in-memory only, lost on restart. **Recommended: (a).** Per-edit objects give the viewer precise hooks for per-block accept / reject UI and survive a reload; whole-file diff is reconstructible from the structured edits, the inverse isn't.

  **Per-edit granularity.** One entry per Edit / Write tool call. Each entry records: a stable id (`pe-<ulid>`), the originating `tool_use_id`, the block signature (track_id + anchor + label + excerpt — same shape `/edit-block` consumes), the pre-edit text, the proposed text, the agent label (provider:model), and a timestamp. A turn that touches three blocks produces three entries.

  **Mode toggle.** Per-doc, not per-session. Stored in frontmatter (`review_mode: pending`) or a sidecar config; surfaced in the viewer as a Review/Live toggle that flips it. Per-session toggles get reset every fresh chat and the reader has to remember which docs deserve scrutiny; per-doc means a paper-in-review can be marked once and stay that way.

  **Agent's view of the doc.** Falls out naturally from the revised hook model — bytes are on disk, so the existing "inline current.md into the chat preamble" path already shows the agent its proposed state. Phase 3 (preamble work) becomes a no-op for the in-turn case; it still matters across turns because pending entries persist until accepted/rejected and the agent on a fresh turn needs to see the pending-applied state as its starting point. Since pending changes ARE on disk, that's just "read current.md" — same as today.

  **Hook routing (revised in phase 2).** The original proposal said the on-disk current.md doesn't move until accept; in implementation that breaks sequential Edits within a turn (Edit 2's `old_string` won't match a reverted state). Revised model: bytes land on disk normally, **pending.json tracks revocability**. PostToolUse fires after each Edit / Write; on success, if review mode is on for that doc, we record a pending entry capturing `old_text` (pre-edit) and `new_text` (post-edit) for the affected scope. Accept = remove the pending entry (changes were already on disk). Reject = restore `old_text` for that scope. Same engineering property the original wording was reaching for; cleaner to reason about.

  **Human inline edit interaction.** Humans win. An inline edit on a block that has a pending agent change *for that block* rejects the pending change as a side effect and writes the human's edit straight to current.md. Two-writer concurrency on the same block is the only case the substrate has to handle; both writers on different blocks is just "two independent pending entries, accept independently."

  **Conflicts agent vs agent.** Successive agent edits on the same block in pending mode: replace the existing pending entry rather than stacking. Otherwise the accept/reject UI has to expose a stack which is more cognitive load than it's worth in v0. Audit trail of intermediate proposals is recoverable from the chat log if anyone wants it.

  **Renderer behavior.** For each pending entry, the viewer overlays a diff-styled wrapper on the affected block: pre-edit shown struck through, proposed shown highlighted, Accept / Reject buttons inline. Whole-block replacement is enough for v0 — character-level diff inside the block is the open follow-up question already captured below as "Sub-block diff granularity."

  **History semantics.** Accept = same as a normal agent edit landing today: PreToolUse snapshot fires, current.md updates, history records the change, pending entry is removed. Reject = no change to current.md and no history entry; pending entry is removed.

  **Validator interaction.** Validators run when the edit is *applied* (Accept), not when it's proposed. Pending entries can contain invalid markdown; the validator gates acceptance. This matches the principle that pending mode is reader-controlled: the reader sees the proposal and decides.

  **Implementation phases.** Roughly: (1) disk format + pending.json read/write helpers, (2) PreToolUse routing when mode is on, (3) preamble applies pending to the agent's view, (4) viewer overlay rendering, (5) Accept / Reject UI + endpoints, (6) human-inline-edit-rejects-pending interaction. Each is a day or two; total roughly a week of work end-to-end. None of these phases ships standalone — pending mode is off by default until phase 5 lands.

  **Open forks to settle during implementation.**
    - Should pending entries reference blocks by track_id only? Lazy-minting a track_id at proposal time would make signature matching trivial but bloats the source with `<!-- id:b-... -->` comments before every touched block. Alternative: keep the signature soup but rebuild it after current.md changes underneath (block excerpts drift as edits land).
    - For block *insertions* and *deletions* (Edits that change block structure, not just content): what does the diff overlay look like? Probably a "new block here" card with Accept-to-insert and a strike-through-with-Accept-to-delete card. Defer until v0 lands; replacements are the common case.
    - Does Reset clear pending? The intuitive answer is yes — Reset means "back to baseline, lose the working state" — but pending isn't really "working state" in the same sense. Probably: Reset clears both current.md *and* pending.json.
    - Per-turn cost cap interacts oddly with pending mode: if the agent is racking up token spend on edits the reader hasn't seen yet, the cap fires mid-turn and the reader has a partially-proposed change. Probably fine for v0; document the behavior.

- **Snapshot semantics: pre-edit vs. post-edit (vs. HEAD-pointer).** Today the substrate is *pre-edit*: the `PreToolUse` hook captures the doc's state right before each agent `Edit`/`Write`. Snap N = "state before edit N." This creates an asymmetry: the *result* of the most recent edit lives only in the working copy, not in any snap. Destructive UI actions (`/reset`, `/undo`, `/restore_snapshot`) used to silently destroy that latest state. **Mitigation in place:** each restore handler captures the current working copy as a safety snap before overwriting, so the History panel can always recover the just-lost state. Known v0 limitation: with safety snaps in `/undo`, double-clicking Undo can read the just-taken safety snap as the new "newest" and produce a "redo" instead of a deeper undo. Accepted as the rare edge case; the common case (single Reset/Undo destroying current state without recourse) is fixed. **Longer-term:** either move the snapshot to `PostToolUse` (snap N = "state after edit N"; latest snap always equals working copy), or introduce a HEAD-pointer model with proper undo/redo (snaps are immutable history; HEAD moves through them; "current state" is a derived concept). The latter is the correct answer for multi-writer work but isn't blocking today.
- **Sub-block diff granularity.** Pending-changes review and any future multi-writer merge needs a notion of "what changed inside this block." Today we have full-block before/after. Question for that feature: do we compute character-level deltas, or is whole-block replacement enough?
- **Anchor ID uniqueness across docs.** With multi-doc workspaces, do `{#rolle}` anchors need to be globally unique, or namespaced by doc slug? Probably the latter — `docs/rolle-paper/current.md#rolle` is the natural cross-doc reference shape.
- **Frozen / republished doc lifecycle.** When a doc is intentionally locked (a finished version distributed for reading), do new tracking IDs mint in the frozen copy, or does a fork happen? Relevant once any "this version is final" UI exists.

## Recently shipped

For context on pace.

- **Per-doc skill — `<section class="agent-skill">` in-body convention + Skills manager UI.** Each doc carries its own working contract — voice, structural conventions, domain rules — as one or more `<section class="agent-skill">…</section>` blocks in the body. Doc view hides them via CSS so the reader sees clean content; the chat-preamble loader notes "this doc has agent-skill sections, treat as authoritative" so the agent absorbs the contract before processing body content. Authorship: the overflow menu's "Skills…" entry opens a manager that lists every section in the active doc with edit / delete / "+ Add new" affordances, each Save going through the standard validator + snapshot + broadcast path. Resolved a design fork the ROADMAP originally left open: in-body section vs separate `docs/<slug>/SKILL.md` file. In-body won — lives next to what it constrains, exports cleanly with the single-file HTML, survives copy/paste, no second file to manage, no sync risk. (If a future need surfaces for shared contracts across multiple docs or contracts too large to fit in-body, revisit then.)
- **Sandboxed Bash for structured-data transforms.** The agent's `allowed_tools` gained `Bash`. Unlocks transposing MusicXML, pivoting CSVs, resizing images — work the agent can't do ergonomically through Edit alone. Enforcement is platform-split: on macOS / Linux / WSL2 the Claude CLI's built-in sandbox (`SandboxSettings(enabled=True, autoAllowBashIfSandboxed=True, network=…)`) closes network by default and syscall-filters file access; on Windows the CLI no-ops (per its own warning) so we fall back to a subprocess wrapper in `sandbox.py` invoked via a PreToolUse hook that rewrites the agent's command. The Windows fallback is best-effort — scoped cwd + curated env (no API keys leak) + timeout — but Git Bash bundles curl/ssh/wget, and Win10+ ships curl in System32, so the no-network claim doesn't hold there. Threat model: personal-machine tool, not airtight. The load-bearing protection on both platforms is curated env (nothing for a prompt-injected agent to exfiltrate); on Unix the network policy is the second layer, on Windows there's no second layer. Validator-side regression at the same time: `<script>` tags whose `type=` is a non-JS MIME (e.g. `application/vnd.recordare.musicxml+xml`) are no longer JS-syntax-checked — they're opaque data per HTML spec. **Operator opt-out:** `python start.py --unsafe-bash` (Windows only) disables the subprocess wrap when the operator trusts every doc in the workspace and wants full agent shell capability. Out-of-band — no in-chat path enables it, so a prompt-injected doc can't social-engineer the user into flipping a toggle mid-conversation.
- **Inline edit + slash-command vocabulary + intro refresh.** Readers can click into a paragraph or heading and type to edit directly — `contenteditable="plaintext-only"` while the agent is idle, locked when the agent is mid-turn. Edits POST to `/edit-block`, which finds the source span via the existing block-identification machinery (`_find_block_span` over track_id / anchor_id / label / excerpt), runs the validator, snapshots, writes. The chat input recognizes a small slash-command vocabulary: `/cancel` (interrupt in-flight turn — backend tracks the asyncio.Task and `task.cancel()`s it on demand; kills the codex subprocess too via the runtime's `_run_codex_process` finally block, so `/cancel` is at full runtime parity), `/help`, `/reset` (routes through the existing confirmation dialog), `/undo`, `/new` / `/clear`, `/model <name>`. Unknown slashes get an error rather than being silently forwarded to the agent. The intro example was rewritten to actually demonstrate today's substrate — new "Edit by hand" try, plus dedicated workflow sections for import / embed / export.
- **Single-file HTML export.** New Export tab next to Print. Bundles the rendered doc body, AM's typography CSS (fetched from `/iframe-host` so it stays in sync with what the viewer renders), KaTeX from CDN, the doc's own `<script>` tags, and every referenced asset as a data: URI. Reader downloads `<slug>.html` and opens it anywhere — math typesets, animations run, no AM or backend required. Pure client-side; same rendering pipeline already running in the viewer.
- **Cross-origin doc iframe for third-party embeds.** The iframe used to be null-origin (`srcdoc`-based, no `allow-same-origin`), which blocked YouTube / Twitter / CodePen / Figma / Observable — those embeds all require same-origin-to-themselves for their player or widget to bootstrap. Switched the iframe to load from a different origin than the viewer on the same port (`localhost:<port>` vs `127.0.0.1:<port>` — different origins per the browser's SOP, single backend listener). Sandbox now includes `allow-same-origin`; safe because same-origin-to-itself ≠ same-origin-to-parent. The 200-line srcdoc string in `index.html` lifted into a real `iframe-host.html` served at its own URL. PostMessage handshake now origin-checked at both ends.
- **Text selection in the doc iframe.** The click handler used to call `removeAllRanges()` + `preventDefault()` on every click to support block-focus, which killed native text selection (drag-to-select, double-click-to-word, triple-click-to-line, copy). Now the handler checks the selection state at click time and falls through to block-focus only when no text is selected; click-on-contenteditable doesn't preventDefault so the caret is placed naturally. Native browser behavior, no JS interference.
- **PDF import.** Drop a `.pdf` onto + Doc and Claude vision converts it server-side via a direct Anthropic API call (no agent loop, no chat round-trip). Heading hierarchy, lists, math (rendered as LaTeX), tables, theorem environments preserved — Claude reads the rendered pages + the text stream and reconstructs structure. ~5–30s per paper. Markitdown is the offline fallback when no API key is set or the PDF is over the 32MB limit; office formats (`.docx`, `.xlsx`, `.pptx`) go straight to markitdown. `AM_PDF_BACKEND=auto|claude|markitdown` env var picks the path; the smoke harness sets `markitdown` so test runs don't burn API tokens.
- **LaTeX import via Claude.** Same pattern as PDF, but with `.tex` source as text (no rendering needed). `\section` → `##`, `\begin{theorem}` → `<section class="theorem">`, math preserved as `$...$` / `$$...$$`, preamble dropped, `\textbf{...}` → `**...**`, etc. `_LLM_CONVERT_TEXT_EXTS` is the extension allowlist; `.rst` / `.org` would go through the same path with a small prompt adjustment when demand shows.
- **Asset drop.** Drop an image / audio / video / data file (csv / json / parquet) onto an open doc and it lands at `docs/<slug>/assets/<file>` with a system-style chat message telling the agent what arrived. The iframe carries a `<base href="/docs/<slug>/">` so the agent can reference assets with relative paths (`<img src="assets/foo.png">`). 25 MB cap; blocklist on executable extensions; assets are gitignored by default (`git add -f` to ship a specific one with an example doc).
- **`preserveStructuralBlocks` narrowed.** Previously stripped blank lines from inside every `<figure>` / `<aside>` / `<section>` / etc. to keep CommonMark from breaking type-6 HTML blocks at the first blank line. Side effect: inner markdown inside a `<section class="theorem">` rendered as literal text (`### Heading` showed up as "### Heading"). Narrowed to `<script>` bodies only — fixes the original figure-with-script blank-line bug without breaking inner markdown in `<section>` / `<aside>` / `<details>` / `<div>` blocks.
- **Staged activity indicator.** Sending… → Thinking… → tool name → hide on the first text chunk. The streaming bubble itself becomes the progress indicator after that, so the strip retires; if the agent goes thinking-or-tool-use *again* after some text, the strip re-creates.
- **Structured content moved to HTML blocks.** Retired the `:::` directive grammar entirely in favor of `<aside class="note">`, `<figure>`, `<section class="theorem">`, `<div class="pinned">`. CommonMark passes HTML blocks through verbatim, CSS targets class names directly. Whole categories of bugs (markdown-it-attrs intercepting `{...}`, parseDirectiveInfo regex edge cases, directive grammar vs HTML grammar) vanish. Renderer shrank by ~170 LoC; the validator dropped its directive checks; the skill collapsed two equivalent representations (heading-form vs directive-form theorems) into one. Browser-smoke gained a `scenario_html_block_structured_content` to lock the patterns.
- **Doc-as-folder layout.** `docs/<slug>/baseline.md` (history-0, tracked), `current.md` (working copy, gitignored), `snaps/` (pre-edit captures, gitignored), `assets/` (materials). Replaces the old `examples/<name>.md` + `.history/<stem>/` split. `.gitignore` is now a structural invariant: only the three ship-with docs' baselines (plus optional provenance + assets) can ever land in commits. WebSocket protocol switched to slug-based (`{doc: "intro"}` instead of file paths). The patches sidecar was retired — diffs can be recomputed from snapshots later if a review pipeline ever needs them.
- **Insert-here position selection.** Clicking in the gap between blocks (rather than on a block) sets an insertion point; the chat preamble names the surrounding blocks and tells the agent to anchor its Edit on one of them. The viewer paints a slim horizontal marker at the chosen gap. Removes "agent guesses from prose" as a positional error mode.
- **Iframe runtime: morphdom + per-script cleanup registry.** Doc updates now diff into the iframe in place — scroll position, in-progress canvas animations, attached listeners all survive edits. `window.__doc.cleanup(fn)` tracks teardowns per-script via a WeakMap; cleanups fire only when the registering script is removed/replaced, so unchanged animations keep running across sibling edits.
- **Doc inlined in chat preamble.** The active doc (up to 50 KB) is read once per turn and injected into the agent's context between fence markers. Saves a `Read` tool round-trip per turn; amortized by prompt caching from turn 2 on. Skill instructs the agent not to `Read` the file unless they've edited it themselves since the preamble.
- **Validator hardened.** JS check switched from `new Function` (function-body grammar) to `node --check` (real script grammar, with line + caret in errors). Markdown code spans and fenced blocks are masked before tag extraction so `<script>` mentioned in prose backticks doesn't false-positive. UTF-8 forced on the subprocess pipe (no more cp1252 crashes on math characters). Server-side log prints the full multi-line error so the operator can see what the agent saw. Three-strike retry cap — after 3 consecutive validation failures on the same file, the loop is durably broken and the agent is told to stop and report.
- **Skill merge.** Side-by-side merge of the AM and AMV3 skills into one tighter, accurate spec covering: how the agent receives context (active doc, focused block, insertion point), what happens on Edit (validator + EDIT REJECTED + retry cap), the iframe runtime inventory (KaTeX, morphdom, `__doc.cleanup`), the cleanup-registry pattern, and the HTML-block conventions for structured content. The `.agents/` mirror auto-syncs at backend boot.
- **UTF-8 launcher + SIGINT watchdog (`start.py`).** Respawns Python with `PYTHONUTF8=1` before any I/O initialises so the Claude SDK's subprocess pipes don't choke on non-Latin-1 characters (`ε`, `π`, `⟹`). Force-exits 3s after Ctrl+C if aiohttp's IOCP cleanup hangs on Windows.
- **Security pass** — iframe sandbox dropped `allow-same-origin` (null-origin doc context, can't read parent or backend); `serve_static` allowlisted to favicon + `docs/<slug>/{current,baseline}.md` only (no more `.env` exposure); `Origin` validation on WS + POST routes; drop-to-convert preview dialog (user sees the first 2KB before the agent does); upload blocklist of executable/binary extensions + NUL-byte guard; Claude's `Bash` and `WebFetch` removed from `allowed_tools` + denied in `.claude/settings.json`; path-validated `pre_tool_use_hook` rejects Claude writes outside `docs/<slug>/current.md`; SKILL.md gained a "Security boundaries" section + the `.agents/` mirror auto-syncs at startup; Codex runtime got a post-turn write-path validator (revert-from-snapshot for any modification to a protected file, with chat warning) and explicit `network_access=false` override. Real-browser + real-API test harnesses make future regressions catchable in <60s — 42 browser-smoke scenarios currently.
- **Codex provider work** — skill injection, chat history replay, substrate parity (snapshots + alias bookkeeping at per-turn granularity), `start.py` launcher with persistence, model-not-supported auto-retry, activity strip mapping.
- **Provider runtime abstraction** — `agent_runtime/` registry, Protocol-based seam, Codex CLI adapter (experimental).
- **v0.1 public release** — adaptive reading (translate / restyle / expand); live in-place DOM patching; click-to-focus + multi-select; version history with restore; multi-view tabs (Doc / Graph / Source / LaTeX / Print); figure programs (canvas / SVG / Desmos); KaTeX math with `\begin{aligned}` rule; AMS-TeX importer; single-file browser deployment; per-doc model selector.
